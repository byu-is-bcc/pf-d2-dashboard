# Upper bounds on purpose. Unpinned dependencies are the most common reason a
# reviewer cannot reproduce a student project a year later, and this repo is
# supposed to still start for a stranger in 2028.
#
# Streamlit Community Cloud installs from this file. Everything here has to be
# pure-pip installable, because there is no apt step on that platform.

streamlit>=1.40,<2
altair>=5.4,<6
pandas>=2.2,<3

# The database is SQLite, which is in the Python standard library. There is no
# database driver in this file and there does not need to be one. That is a
# feature: nothing to provision, nothing to pay for, nothing to leak.

# Used by scripts/refresh.py when you point fetch() at a real HTTP source.
# Nothing in the shipped seed path needs it, so if you never leave the seed you
# can delete this line.
requests>=2.32,<3
